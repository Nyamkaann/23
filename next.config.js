/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['placehold.co'], // Add the domain for the placeholder image
  }
}

module.exports = nextConfig 